# Podfile — base from 3dReal, added WebKit
target '3d-bereal' do
  use_frameworks!

  pod 'Firebase/Analytics'
  pod 'Firebase/Auth'
  pod 'Firebase/Firestore'
  pod 'Firebase/Storage'
  pod 'Firebase/Messaging'
  # NEW: WebKit bundled in iOS SDK, no extra pod needed
  # AVKit also bundled — no extra pod needed
end
